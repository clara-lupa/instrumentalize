<div class="container">
  <h1 class="text-color"> All instruments </h1>
  <ul>
  <% @instruments.each do |instrument| %>
  <li>
    <%= instrument.name %>
    <%= link_to "Details", instrument_path(instrument) %>
    <% if policy(instrument).edit? %>
      <%= link_to "Update", edit_instrument_path(instrument) %>
      <%= link_to "🗑", instrument_path(instrument), method: :delete %>
    <% end %>
  </li>
  <% end %>
  </ul>

  <% if policy(Instrument).create? %>
  <%= link_to "New instrument", new_instrument_path, class:'button-pink' %>
  <% end %>
</div>
